<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Image de fond -->
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/background"
        android:scaleType="centerCrop"
        android:contentDescription="@null" />
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Conteneur qui occupe tout l'écran -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:weightSum="7"
            android:paddingTop="12dp">

            <!-- Texte en en-tête -->
            <TextView
                android:id="@+id/headerText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Au rythme de la production"
                android:textSize="40sp"
                android:textStyle="bold"
                android:fontFamily="@font/bebas_neue"
                android:textColor="@android:color/black"
                android:paddingTop="12dp"
                android:gravity="center"/>

            <TextView
                android:id="@+id/headerText2"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Animateur"
                android:textSize="34sp"
                android:textStyle="bold"
                android:fontFamily="@font/bebas_neue"
                android:textColor="@android:color/black"
                android:layout_marginBottom="32dp"
                android:gravity="center"/>

            <!-- Grille de boutons -->
            <GridLayout
                android:id="@+id/buttonGrid"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:columnCount="1"
                android:rowCount="5"
                android:alignmentMode="alignMargins"
                android:useDefaultMargins="true"
                android:padding="16dp">

            <!-- Liste des boutons -->
                <Button
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:text="Vérifier connexions"
                    android:id="@+id/verifierconnexion"
                    android:fontFamily="@font/bebas_neue"
                    android:textColor="@android:color/white"
                    android:backgroundTint="@null"
                    android:background="@drawable/rounded_button"/>

                <Button
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:text="Afficher statistiques"
                    android:id="@+id/afficherstatistiques"
                    android:fontFamily="@font/bebas_neue"
                    android:textColor="@android:color/white"
                    android:backgroundTint="@null"
                    android:background="@drawable/rounded_button"/>

                <Button
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:text="Suggestions Améliorations"
                    android:id="@+id/suggestionsamelioration"
                    android:fontFamily="@font/bebas_neue"
                    android:textColor="@android:color/white"
                    android:backgroundTint="@null"
                    android:background="@drawable/rounded_button"/>

                <Button
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:text="Boîte de sons"
                    android:id="@+id/boitedeson"
                    android:fontFamily="@font/bebas_neue"
                    android:textColor="@android:color/white"
                    android:backgroundTint="@null"
                    android:background="@drawable/rounded_button"/>

                <Button
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_columnWeight="1"
                    android:text="Statistiques par poste"
                    android:id="@+id/statistiquesparposte"
                    android:fontFamily="@font/bebas_neue"
                    android:textColor="@android:color/white"
                    android:backgroundTint="@null"
                    android:background="@drawable/rounded_button"/>

            </GridLayout>

            <ImageButton
                android:id="@+id/returnbutton"
                style="@style/SoundButtonStyle"
                android:src="@drawable/returnbutton"
                android:background="@null"
                android:contentDescription="Retour à l'activité précédente"
                android:layout_marginTop="16dp"/>

        </LinearLayout>
    </ScrollView>
</FrameLayout>